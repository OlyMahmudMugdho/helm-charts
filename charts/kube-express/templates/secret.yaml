# secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
type: {{ .Values.secret.type }}
data:
  {{ .Values.secret.data.key }}: {{ .Values.secret.data.value }} # Base64 encoded value of 'mynewsecretvalue'