apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kube-express.fullname" . }}-test-connection"
  labels:
    {{- include "kube-express.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kube-express.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
